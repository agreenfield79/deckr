fastapi
uvicorn[standard]
slowapi
python-dotenv
requests
python-multipart
ibm-cos-sdk          # Step 12.7 — IBM Cloud Object Storage adapter
ibm-watsonx-ai       # Step 13.2 — SDK for streaming (generate_text_stream); Step 14.2 Embeddings
pypdf                # Step 14.1 — PDF text extraction pass 1 (fast, lightweight)
pdfminer.six         # Step 14.1 — PDF text extraction pass 2 (complex layouts, custom font encoding)
pymupdf              # Step 14.1 — PDF text extraction pass 3 (MuPDF engine; handles compressed/CJK PDFs)
# Phase 12 note: Orchestrate runtime invocation uses `requests` (already listed above).
# The ADK CLI (`pip install ibm-watsonx-orchestrate`) is a dev-machine tool for importing
# agents into Orchestrate — it is NOT a runtime dependency and does NOT belong here.
# Optional: pip install sentence-transformers — enables cross-encoder re-ranking in
# embeddings_service.rerank(). Not listed here due to heavy torch dependency.

# Phase 28 — Neural SLACR model (backend/ml/neural_slacr.py)
# Install in the backend venv: pip install scikit-learn numpy shap lime
scikit-learn     # RandomForestClassifier, MLPClassifier
numpy            # array math, correlation matrix
shap             # TreeExplainer for SHAP waterfall values
lime             # LimeTabularExplainer for local linear approximation

# Phase 4 — Database layer (D-1: SQLite default; PostgreSQL requires Docker)
sqlalchemy>=2.0          # ORM + session factory; create_all() for local SQLite
psycopg2-binary          # PostgreSQL driver (needed when DB_URL=postgresql://...)
pgvector                 # vector column type for SQLAlchemy (PostgreSQL only)
alembic                  # SQL schema migrations (cloud/Docker path; not required for SQLite)
pymongo                  # MongoDB driver (local + Atlas)
google-cloud-firestore   # cloud Firestore (cloud mode only)
networkx                 # in-memory graph (zero-infra fallback; not currently wired)
neo4j                    # Bolt driver — local Neo4j and AuraDB
chromadb                 # local vector store (auto-creates data/.chroma/ on first write)
httpx                    # async HTTP client (SerpAPI, OpenCorporates, CourtListener — Phase 10B)
tenacity                 # retry/backoff for transient SQL connection failures (P-3)
