# SSE base URL — points to the Deckr backend.
# For local development, keep this as localhost:8000.
# For Code Engine production deployment (Phase 19), replace with your Code Engine app URL.
VITE_SSE_BASE_URL=http://localhost:8000
